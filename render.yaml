services:
  - type: web
    name: brahmastra-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn bs_topper.wsgi:application"
    envVars:
      - key: DEBUG
        value: False
